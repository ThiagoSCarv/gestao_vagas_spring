version: "3.8"

services:
  postgres:
    image: postgres:16
    container_name: gestao_vagas_postgres
    environment:
      POSTGRES_DB: gestao_vagas
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
    ports:
      - "5432:5432"
